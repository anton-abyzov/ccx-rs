[package]
name = "ccx-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core agent loop (placeholder)"

[dependencies]
ccx-api = { path = "../ccx-api" }
tokio = { workspace = true }
